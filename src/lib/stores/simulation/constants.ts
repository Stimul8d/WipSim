export const SIM_SPEED = {
    TICK_MS: 1000,      // How often we update
    PROGRESS_PCT: 20    // Progress per tick  
}

export const SIM_TIME = {
    TICK_HOURS: 4,      // Each tick represents 4 work hours
    MS_PER_HOUR: 3600000,
    TICKS_PER_TASK: 5   // Average task takes 20 hours
}